#include <iostream>

int main()
{
    int main() {
        float grade;
        std::cout << "Enter the grade: ";
        std::cin >> grade;

        if (grade >= 5.50) {
            std::cout << "Excellent!" << std::endl;
        }

        return 0;
}